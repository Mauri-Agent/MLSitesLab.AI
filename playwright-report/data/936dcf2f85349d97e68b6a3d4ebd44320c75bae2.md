# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: responsive.spec.ts >> Responsive Design >> Viewport meta tag is present
- Location: e2e/responsive.spec.ts:47:3

# Error details

```
Error: page.goto: net::ERR_CONNECTION_REFUSED at http://localhost:5173/
Call log:
  - navigating to "http://localhost:5173/", waiting until "load"

```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('Responsive Design', () => {
  4  |   test.beforeEach(async ({ page }) => {
> 5  |     await page.goto('/');
     |                ^ Error: page.goto: net::ERR_CONNECTION_REFUSED at http://localhost:5173/
  6  |   });
  7  | 
  8  |   test('Document scrollWidth <= window clientWidth (No overflow-x on Mobile)', async ({ page, isMobile }) => {
  9  |     test.skip(!isMobile, 'This test is only for Mobile');
  10 |     const scrollWidth = await page.evaluate(() => document.documentElement.scrollWidth);
  11 |     const clientWidth = await page.evaluate(() => document.documentElement.clientWidth);
  12 |     expect(scrollWidth).toBeLessThanOrEqual(clientWidth);
  13 |   });
  14 | 
  15 |   test('Document scrollWidth <= window clientWidth (No overflow-x on Desktop)', async ({ page, isMobile }) => {
  16 |     test.skip(!!isMobile, 'This test is only for Desktop');
  17 |     const scrollWidth = await page.evaluate(() => document.documentElement.scrollWidth);
  18 |     const clientWidth = await page.evaluate(() => document.documentElement.clientWidth);
  19 |     expect(scrollWidth).toBeLessThanOrEqual(clientWidth);
  20 |   });
  21 | 
  22 |   test('Header elements remain within bounds on Mobile', async ({ page, isMobile }) => {
  23 |     test.skip(!isMobile, 'This test is only for Mobile');
  24 |     const header = page.locator('header');
  25 |     if (await header.count() > 0) {
  26 |       const headerBox = await header.first().boundingBox();
  27 |       const clientWidth = await page.evaluate(() => document.documentElement.clientWidth);
  28 |       if (headerBox) {
  29 |         // Allow a 1px margin of error for fractional pixels
  30 |         expect(headerBox.x + headerBox.width).toBeLessThanOrEqual(clientWidth + 1);
  31 |       }
  32 |     }
  33 |   });
  34 | 
  35 |   test('Main section elements remain within bounds on Mobile', async ({ page, isMobile }) => {
  36 |     test.skip(!isMobile, 'This test is only for Mobile');
  37 |     const main = page.locator('main');
  38 |     if (await main.count() > 0) {
  39 |       const mainBox = await main.first().boundingBox();
  40 |       const clientWidth = await page.evaluate(() => document.documentElement.clientWidth);
  41 |       if (mainBox) {
  42 |         expect(mainBox.x + mainBox.width).toBeLessThanOrEqual(clientWidth + 1);
  43 |       }
  44 |     }
  45 |   });
  46 | 
  47 |   test('Viewport meta tag is present', async ({ page }) => {
  48 |     const viewportMeta = page.locator('meta[name="viewport"]');
  49 |     await expect(viewportMeta).toHaveCount(1);
  50 |     await expect(viewportMeta).toHaveAttribute('content', /width=device-width/);
  51 |   });
  52 | });
  53 | 
```